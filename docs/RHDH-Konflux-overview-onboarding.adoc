# Building and Testing with RH Internal Konflux

## Triggering and watching builds

See link:RHDH-Konflux-user-guide.adoc[RHDH-Konflux-user-guide.adoc].

## Onboarding a new person

1. Ask that person to go to https://konflux-ui.apps.stone-prod-p02.hjvn.p1.openshiftapps.com/ns/rhdh-tenant/applications
2. Ask that person to log in, register, click "join the waitlist", refresh their browser
3. Ask that person for their username / default workspace. This is likely their kerberos username, but might differ.
4. Add that person here: https://konflux-ui.apps.stone-prod-p02.hjvn.p1.openshiftapps.com/ns/rhdh-tenant/access
5. Once added, you can change their access level to contributor, maintainer, or admin. 

## Onboarding a new y-stream release

Once a quarter, new branch are created in GitHub called `release-1.y` for the new release. 
In the link:https://gitlab.cee.redhat.com/rhidp/rhdh[rhidp/rhdh Gitlab repo], an equivalent branch called `rhdh-1.y-rhel-9` is created.
Once these exist, a new Application, with 3 (or more) components can then be added. 

### Component Nudging

Currently, we have rules in place in link:../build/ci/sync-midstream.sh[sync-midsteam.sh] such that when a new hub or operator image is published, a new bundle will then be created and once that's done, new FBCs will be published. See also link:../build/ci/update-bundle-and-FBCs.sh[update-bundle-and-FBCs.sh].

## File Based Catalogs (FBCs)

FBCs replace IIBs (index bundles) in RHDH 1.4+.

### Building a new FBC or set of FBCs

See link:../build/scripts/renderCatalogs.sh[renderCatalogs.sh].

### Adding a new Application and Component to build FBCs for a new OCP version

This should only be done once every 4mo, when a new OCP version is released and a new version of registry.redhat.io/openshift4/ose-operator-registry-rhel9 becomes available. When creating a new branch, link:https://gitlab.cee.redhat.com/rhidp/rhdh/-/blob/rhdh-1-rhel-9/build/scripts/tagRelease.sh[build/scripts/tagRelease.sh] will automatically add a new application and component for any new OCP version.

### Removing support for EOL OCP versions

When support for an older version of OCP is to be dropped, FBC publishing for that old version can be stopped.

1. Delete the fbc application

2. Update lifecycle docs & official product docs, release notes, etc. 

3. Update defaults in link:https://gitlab.cee.redhat.com/rhidp/rhdh/-/blob/rhdh-1-rhel-9/build/scripts/renderCatalogs.sh[build/scripts/renderCatalogs.sh] (and cherrypick to other branches if applicable)

## Installation

### via Helm

Helm charts for GA releases are manually created. See https://gitlab.cee.redhat.com/rhidp/rhdh/-/blob/rhdh-1-rhel-9/build/helm/prepare.sh

Each CI build produces a helm chart pointing back to that new build. As of 1.5, charts are here: https://quay.io/rhdh/chart

### via Operator

See link:RHDH-Konflux-user-guide.adoc[RHDH-Konflux-user-guide.adoc].

See installation advice and scripts at https://github.com/redhat-developer/rhdh-operator/blob/main/.rhdh/

* install an IDMS/ICSP to resolve container image references
* install the latest FBC as a CatalogSource

## GA Releases

Release Plans are here: https://konflux-ui.apps.stone-prod-p02.hjvn.p1.openshiftapps.com/ns/rhdh-tenant/release

Release Plan Admissions are here: https://konflux-ui.apps.stone-prod-p02.hjvn.p1.openshiftapps.com/ns/rhtap-releng-tenant/release/release-plan-admission?name=rhdh

NOTE: To get access to the rhtap-releng namespace, see https://konflux.pages.redhat.com/docs/users/releasing/preparing-for-release.html#_get_access_to_the_rhtap_releng_managed_workspace

If you need to delete something here's the easiest views:

* RPAs: https://console-openshift-console.apps.stone-prod-p02.hjvn.p1.openshiftapps.com/search/ns/rhtap-releng-tenant?kind=appstudio.redhat.com%7Ev1alpha1%7EReleasePlan%2Cappstudio.redhat.com%7Ev1alpha1%7EReleasePlanAdmission%2Cappstudio.redhat.com%7Ev1alpha1%7ERelease&orderBy=asc&sortBy=Created&name=rhdh
* RPs: https://console-openshift-console.apps.stone-prod-p02.hjvn.p1.openshiftapps.com/search/ns/rhdh-tenant?kind=appstudio.redhat.com%7Ev1alpha1%7EReleasePlan&orderBy=asc&sortBy=Created&name=

Releases can be seen here (with details on if they passed/failed): 

https://console-openshift-console.apps.stone-prod-p02.hjvn.p1.openshiftapps.com/search/ns/rhdh-tenant?kind=appstudio.redhat.com%7Ev1alpha1%7ERelease&orderBy=asc&sortBy=Created&name=

Note that RPs must reference their associated RPAs or else we're not allowed to release. 

### How do I release?

Simply put, to release containers and then FBCs, use this script:

* link:../build/scripts/kfuxRelease.sh[kfuxRelease.sh]

Make sure you have access to the rhtap-releng namespace! 

NOTE: To get access to the rhtap-releng namespace, see https://konflux.pages.redhat.com/docs/users/releasing/preparing-for-release.html#_get_access_to_the_rhtap_releng_managed_workspace
