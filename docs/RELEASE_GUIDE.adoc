 # Release Guide

This document seeks to explain the whole end-to-end process for releasing RHDH, from branch creation to GA push.

_TODO_: Think about how best to create a release guide (gantt chart, smartsheet, etc.)

## Code Freeze / Blockers Only / New Stream Day

### Create branches (eg., 1.1.x, 1.2.x)

. Create Github branches - _TODO_ script this, see https://issues.redhat.com/browse/RHIDP-931

.. [ ] https://github.com/janus-idp/backstage-plugins/branches/all?query=1.&lastTab=overview
.. [ ] https://github.com/janus-idp/backstage-showcase/branches/all?query=1.&lastTab=overview
.. [ ] https://github.com/janus-idp/operator/branches/all?query=1.&lastTab=overview

... _TODO_: do we need branches in https://github.com/redhat-developer/rhdh-chart ?

. Create new branches in pkgs.devel (eg., rhdh-1.1-rhel-9, rhdh-1.2-rhel-9)

.. [ ] https://pkgs.devel.redhat.com/cgit/containers/rhdh-hub/tree/?h=rhdh-1.1-rhel-9
.. [ ] https://pkgs.devel.redhat.com/cgit/containers/rhdh-operator/tree/?h=rhdh-1.1-rhel-9
.. [ ] https://pkgs.devel.redhat.com/cgit/containers/rhdh-operator-bundle/tree/?h=rhdh-1.1-rhel-9

. Request new Brew Targets & product variant (PV) be created via link:https://issues.redhat.com/browse/SPMM-15866[SPMM ticket like SPMM-15866] and by pinging @ahills / @siege on slack

. Once the associated Errata PV is created, ProdSec gets notified and adds the appropriate CPE within a day or two. For questions, ping Martin Prpič or link:https://docs.engineering.redhat.com/pages/viewpage.action?spaceKey=PRODSEC&title=CPE[send an email] to prodsec-request@redhat.com.
 

### Update main branch versions

Submit PRs to fix versions in main.

. [ ] janus-idp/backstage-plugins (_TODO_: migrate to link:https://github.com/backstage/community-plugins[backstage/community-plugins])
.. [ ] bump package.json to new version 1.y+1

. [ ] janus-idp/backstage-showcase (_TODO_: migrate to link:https://github.com/redhat-developer/red-hat-developer-hub[rhdh])
.. [ ] bump link:https://github.com/janus-idp/backstage-showcase/blob/main/package.json#L3[package.json] and e2e-tests/package.json to new 1.y+1 version
.. [ ] verify https://github.com/janus-idp/backstage-showcase/blob/main/packages/app/src/build-metadata.json is correct (both Backstage and RHDH versions)

.. _TODO_: script this step? 

. [ ] janus-idp/operator (_TODO_: migrate to link:https://issues.redhat.com/browse/RHIDP-1021[rhdh-operator])
.. [ ] bump versions to 0.y+1 upstream and 1.y+1 downstream (link:https://github.com/janus-idp/operator/tree/main/.rhdh[.rhdh folder]):
... Makefile
... bundle/manifests/backstage-operator.clusterserviceversion.yaml
... config/manager/kustomization.yaml
... .rhdh/bundle/manifests/rhdh-operator.csv.yaml

NOTE: Dockerfile versions are derived from the backstage-showcase package.json version.

.. __TODO__: script this step? 

### Update rhdh-*-rhel-9 branch versions

Submit PRs to fix version references in `upstream_repos.yml` for both the new rhdh-1.yy-rhel-9 and the rhdh-1-rhel-9 branch.

Specifically:

* link:https://gitlab.cee.redhat.com/rhidp/rhdh/-/commit/ea34860faf4d976e2bd3920df1d0e781808f16a0[do not use main for 1.yy branch]
* link:https://gitlab.cee.redhat.com/rhidp/rhdh/-/commit/81d98cb9a865b3b92eb75ce8113b2019fb241f1e[bump 1.yy-1 to 1.yy]

TODO: since we currently have to update both branches on code freeze, maybe it's more efficient to always only use `main` in `rhdh-1-rhel-9` and to only use `1.yy.x` in `rhdh-1.yy-rhel-9` branch? Then we only need ONE PR when we create the stable upstream branches.

### Trigger respins 

. Create new gitlab pipeline schedule for 1.2 (to run in parallel with 1.x)

. Run pipelines from
.. https://gitlab.cee.redhat.com/rhidp/rhdh/-/pipeline_schedules or 
.. https://gitlab.cee.redhat.com/rhidp/rhdh/-/pipeline (by committing to the branches)

. verify that after moving to 1.2 in main, we have:
.. 1.1 == latest
.. 1.2 == next

... [ ] https://quay.io/repository/rhdh/rhdh-hub-rhel9?tab=tags
... [ ] https://quay.io/repository/rhdh/rhdh-rhel9-operator?tab=tags
... [ ] https://quay.io/repository/rhdh/rhdh-operator-bundle?tab=tags
... [ ] https://quay.io/repository/rhdh/iib?tab=tags

. perform install smoke tests
.. helm chart from https://github.com/rhdh-bot/openshift-helm-charts/tree/rhdh-1-rhel-9/installation#installation and https://github.com/rhdh-bot/openshift-helm-charts/tree/rhdh-1.1-rhel-9/installation#installation
.. operator install from https://gitlab.cee.redhat.com/rhidp/rhdh/-/blob/rhdh-1-rhel-9/build/scripts/installCatalogSourceFromIIB.sh for `--latest` and `--next`

. Push the latest images into the errata in progress

. Verify CHI grades from the Container page; if needed, update to a newer base image or explicitly install patched RPMs, and respin 

. _TODO_ rebuild the bundle, and push that to a SECOND errata for the stable-1.y channel

## During GA week

. Push errata(s) to stage for QE review
. Remind docs they must review/approve the errata
. Collect GO/NO-GO votes from stakeholders
..  _TODO_: Collect list of owners from various stakeholders (Linda's Go/No-Go list)

. Remind QE to move the errata to REL_PREP **ONLY WHEN ALL TESTS ARE DONE**
.. GA push will go out automatically via Release Driver

## On GA Day

. link:https://gitlab.cee.redhat.com/rhidp/rhdh/-/blob/rhdh-1-rhel-9/build/helm/prepare.sh[Push helm chart release]
.. Verify install works via clusterbot smoke test

. Monitor container image release(s) via errata(s)

. Create tags in 6 repos (plugins, showcase, operator, rhdh-chart, rhdh midstream gitlab, rhdh docs GH) - _TODO_ script this, see https://issues.redhat.com/browse/RHIDP-931


### Once errata(s) are live

. Smoke tests
.. Operator: smoke test the ability to install from `stable` and upgrade to `stable-1.y` operator subscription channel; after 1.2 is live, smoke test a 1.1 to 1.2 upgrade test too
.. Helm Chart: smoke test the ability to install or upgrade via a newer chart

. Review websites
.. Developers.redhat.com
.. Access.redhat.com (docs, other)
.. Customer portal

. Check in with marketing re:
.. Internal announcement? External announcement? Other marketing / blogs / videos? Summit alignment / presentations?

## Other considerations

Ongoing through the quarter, remember to:

. Monitor GH-GL syncs and pipelines, as things can fail unexpectedly in OSBS or on gitlab runners
. Monitor quay image updates to ensure everything is kept current and fresh relative to upstram branches

