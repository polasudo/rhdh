# Gitlab builder images

The Dockerfiles in this folder are used to create images that are then used for gitlab runners.

For 1.5+ using node22:

```
TODAY=$(date +%Y%m%d-%H%M)
podman rmi registry.redhat.io/ubi9:latest -f
podman build . --no-cache -f build/dockerfiles/yarn3-node22-ubi9.Dockerfile --label quay.expires-after=99d \
    -t quay.io/rhdh/gitlab-runner:yarn3-node22-$TODAY \
    -t quay.io/rhdh/gitlab-runner:yarn3-node22-latest 
podman push quay.io/rhdh/gitlab-runner:yarn3-node22-$TODAY
podman push quay.io/rhdh/gitlab-runner:yarn3-node22-latest
```

For 1.4 and node20:

```
TODAY=$(date +%Y%m%d-%H%M)
podman rmi registry.redhat.io/ubi9:latest -f
podman build . --no-cache -f build/dockerfiles/yarn3-node20-ubi9.Dockerfile --label quay.expires-after=99d \
    -t quay.io/rhdh/gitlab-runner:yarn3-node20-$TODAY \
    -t quay.io/rhdh/gitlab-runner:yarn3-node20-latest 
podman push quay.io/rhdh/gitlab-runner:yarn3-node20-$TODAY
podman push quay.io/rhdh/gitlab-runner:yarn3-node20-latest
```
