upstream:
  nameOverride: developer-hub
  backstage:
    image:
      registry: registry.redhat.io
      repository: rhdh/rhdh-hub-rhel9
      # Digest is set via prepare.sh script
      # version: "${RHDH_DIGEST_RRIO}"
  postgresql:
    image:
      registry: registry.redhat.io
      repository: rhel9/postgresql-15
      version: "${POSTGRESQL_DIGEST}"
